# ITINERA 云端部署指南 🚀

## 📋 部署到 Streamlit Cloud

### 步骤 1: 准备 GitHub 仓库
1. 在 GitHub 上创建新仓库（建议命名为 `itinera-travel-planner`）
2. 将 `ITINERA_Cloud` 文件夹中的所有文件上传到仓库

### 步骤 2: 部署到 Streamlit Cloud
1. 访问 [share.streamlit.io](https://share.streamlit.io)
2. 使用 GitHub 账号登录
3. 点击 "New app"
4. 选择您的 GitHub 仓库
5. 主文件路径设置为: `app.py`
6. 点击 "Deploy!"

### 步骤 3: 获取网址
部署完成后，您将获得类似这样的网址：
`https://your-username-itinera-travel-planner-app-xxxxx.streamlit.app`

## 🌐 其他部署选项

### Heroku 部署
1. 安装 Heroku CLI
2. 创建 Procfile: `web: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0`
3. 部署命令:
   ```bash
   git init
   heroku create your-app-name
   git add .
   git commit -m "Deploy ITINERA"
   git push heroku main
   ```

### Railway 部署
1. 访问 [railway.app](https://railway.app)
2. 连接 GitHub 仓库
3. 自动检测为 Python 项目
4. 部署完成

### Render 部署
1. 访问 [render.com](https://render.com)
2. 连接 GitHub 仓库
3. 选择 "Web Service"
4. 构建命令: `pip install -r requirements.txt`
5. 启动命令: `streamlit run app.py --server.port=$PORT --server.address=0.0.0.0`

## ⚙️ 配置说明

### 环境变量
如果需要设置环境变量，可在各平台的设置中添加：
- `PORT`: 端口号（通常自动设置）
- 其他自定义配置

### 域名自定义
- **Streamlit Cloud**: 免费版使用默认域名
- **付费版**: 可以设置自定义域名

## 🔧 故障排除

### 常见问题
1. **依赖安装失败**: 检查 requirements.txt 格式
2. **端口问题**: 确保使用 $PORT 环境变量
3. **内存不足**: 考虑优化代码或升级套餐

### 性能优化
1. 使用 `@st.cache_data` 缓存数据
2. 减少重复计算
3. 优化数据结构

## 📊 监控和维护

### 日志查看
- Streamlit Cloud: 在控制台查看部署日志
- Heroku: `heroku logs --tail`

### 更新部署
只需将更新推送到 GitHub 仓库，平台会自动重新部署。

## 💰 成本考虑

### 免费选项
- **Streamlit Cloud**: 免费，但有使用限制
- **Railway**: 500小时/月免费
- **Render**: 750小时/月免费

### 付费升级
当访问量增加时，考虑升级到付费计划以获得更好的性能和稳定性。

---

**推荐使用 Streamlit Cloud 进行快速部署！** 🌟